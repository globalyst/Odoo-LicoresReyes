<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="offers.list">
      <field name="name">offers list</field>
      <field name="model">offers.offers</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="offers.action_window">
      <field name="name">offers window</field>
      <field name="res_model">offers.offers</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="offers.action_server">
      <field name="name">offers server</field>
      <field name="model_id" ref="model_offers_offers"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="offers" id="offers.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="offers.menu_1" parent="offers.menu_root"/>
    <menuitem name="Menu 2" id="offers.menu_2" parent="offers.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="offers.menu_1_list" parent="offers.menu_1"
              action="offers.action_window"/>
    <menuitem name="Server to list" id="offers" parent="offers.menu_2"
              action="offers.action_server"/>
    -->
  </data>
</openerp>